[package]
name = "server"
version = "2.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.40", features = ["derive"] }
env_logger = "0.11.8"
rocket = "0.5.1"
serde = { version = "1.0.219", features = ["derive"] }
glob = "0.3.2"
log = "0.4.27"

lib = { path = "../lib" }
